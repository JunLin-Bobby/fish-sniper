# Strategy Report — 後端 / LLM Payload 待辦

> **Payload v2 核心欄位已上線**（`todays_pattern`、`confidence_pct`、`holding_zones`、`tactical_role` + `reason`）。報告頁已直接消費 API 回傳值。

## 仍待後續版本

### 1. Holding zone 簡圖

**可選：** `holding_zone_diagram_hint`（岸線 / 水深簡圖結構化座標）供前端 SVG 渲染。

### 2. Conditions 精簡模式

**可選：** 後端回傳 `conditions_summary_one_liner` 供 hero 或側欄單行摘要，減少與 mission brief 重複感。

---

*最後更新：Strategy Report Payload v2 — 步驟六、七完成。*
